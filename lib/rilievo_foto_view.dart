import 'package:flutter/widgets.dart';

class RilievoFotoView extends StatelessWidget {
  const RilievoFotoView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}